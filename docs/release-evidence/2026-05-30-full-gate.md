# Full Gate Evidence — 2026-05-30

Release-readiness evidence for the Python `moose-inventory` package before any public package release.

## Scope

- Package: `moose-inventory`
- Version: `0.0.0`
- Branch under verification: `master`
- Verification commit: `1185df7eba1080c1c7185c18a81bee1c8ae15ca4`
- Evidence recorded from branch: `release/full-gate-evidence`
- Timestamp: `2026-05-30T04:33:44-04:00`
- Publication status: no PyPI upload, release tag, or public package publication was performed.

## Local gate

Command:

```bash
./scripts/check.sh
```

Result: passed.

Summary from the gate run:

- `pytest`: 137 passed in 59.61s.
- `ruff check src tests`: passed.
- `mypy`: `Success: no issues found in 11 source files`.
- `python -m build`: successfully built sdist and wheel.
- `twine check dist/*`: passed for both generated artifacts.

A prior targeted verification pass for the dry-run/plan work also passed:

```bash
git diff --check
python -m pytest tests/test_cli_host.py tests/test_cli_group.py tests/test_cli_tags.py -q
python -m ruff check src tests
```

Result: 13 passed; Ruff clean; diff whitespace clean.

## Ruby/Python parity status

The full gate includes the Ruby/Python compatibility suite and parity manifest tests.

- `tests/test_parity_manifest.py`: passed.
- `tests/test_ruby_python_compat.py`: passed.

## Built artifacts

Artifacts generated under `dist/`:

```text
87b397df8dff402d47a1a1a768d66a5d861dbc73876cbf737cd6654969084d60  dist/moose_inventory-0.0.0-py3-none-any.whl
b4394825ca96dd6750515c0c9609001e2d24d93f32ea1e839a1677bb789d2f59  dist/moose_inventory-0.0.0.tar.gz
```

## Backend smoke evidence

The default test suite exercises SQLite and validates MySQL/MariaDB and PostgreSQL adapter URL/config handling through smoke coverage, but no live MySQL/MariaDB or PostgreSQL service connection was run for this evidence packet.

Before a public release, run real-service backend smoke checks if those services are available and append the results here or in a release-specific evidence file.

## Known limitations / release blockers

- Version remains `0.0.0`; choose and update the public release version before publishing.
- Real MySQL/MariaDB and PostgreSQL service smoke checks are not recorded in this packet.
- No release tag or PyPI publication has been performed.
