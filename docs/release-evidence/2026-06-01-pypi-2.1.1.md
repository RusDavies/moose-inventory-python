# PyPI Release Evidence — 2026-06-01 — 2.1.1

Package: `moose-inventory`
Version: `2.1.1`
Repository: `RusDavies/moose-inventory-python`
Published commit: `c56f0accfcf414d81bab76af39fd417ff2808422`
Publish workflow: `.github/workflows/publish.yml`

## Purpose

Patch release to polish the PyPI project README after inspecting the live 2.1.0 PyPI page.

Changes:

- Converted raw internal documentation paths into GitHub links.
- Added direct install and CLI smoke commands.
- Simplified command blocks for cleaner PyPI rendering.
- Kept Trusted Publishing as the documented release path.

## Trusted Publishing

Publication used PyPI Trusted Publishing through GitHub Actions OIDC.

- PyPI workflow run: <https://github.com/RusDavies/moose-inventory-python/actions/runs/26779927888>
- Target: `pypi`
- Requested version: `2.1.1`

## Gates

The PyPI workflow run completed successfully:

- Version metadata check passed for `pyproject.toml` and `src/moose_inventory/version.py`.
- Release-readiness gate passed: tests, Ruff, mypy, package build, and Twine check.
- `pip-audit . --progress-spinner=off` passed.
- OSV Scanner `scan source --recursive --no-ignore .` passed.
- Gitleaks full-history scan passed.
- Final rebuild and `twine check dist/*` passed.

## Published artifacts

Hashes from the PyPI workflow final rebuild:

```text
76b5314d9f447493a1b151933fc1ab16d9369050c705389e40063cc7161c2846  dist/moose_inventory-2.1.1-py3-none-any.whl
3551eeec42cd43e7837be53fa6f531117c9a48dd3c846be6d4a0e3a1032e390e  dist/moose_inventory-2.1.1.tar.gz
```

The publish action generated PyPI attestations for both artifacts.

## Install and page verification

Verified from real PyPI in a clean virtual environment:

```text
pip install moose-inventory==2.1.1
Successfully installed moose-inventory-2.1.1
```

Import/version smoke:

```text
from moose_inventory.version import __version__  # 2.1.1
moose-inventory version                         # Version 2.1.1
```

Live PyPI page inspection confirmed `moose-inventory 2.1.1` is the latest release and the README links render as clickable GitHub links.
